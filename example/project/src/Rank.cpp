#include<ui_engine.h>

using namespace std;

void Rank() {

    INIT_NORMAL
    make_boundary(59,22,LINE);
    
    LOOP(
        // LISTEN_BUTTON()
        powerprint("Game", 25, 5, aqua, -1);
    )
    return;
}